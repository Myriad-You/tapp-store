var STORE='minecraft-hub.servers.v1',HOME_STORE='minecraft-hub.home-server.v1',VERSION_STORE='minecraft-hub.java-versions.v1',state={items:[],last:null,home:null,isAdmin:false,querySeq:0,versions:null,versionsSource:'builtin',versionsLoading:false,versionCatalogs:{},versionSources:{},detailSeq:0};function $(id){return document.getElementById(id)}function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}async function note(m,t){try{await Tapp.ui.showNotification({title:t==='error'?'查询失败':'Minecraft',message:m,type:t||'success',duration:3000})}catch(e){}}function validAddress(v){return /^[a-z0-9.-]+(?::\d{1,5})?$/i.test(v)&&!/^[-.:]|[-.:]$/.test(v)}function motd(x){if(!x)return'';if(typeof x==='string')return x;if(Array.isArray(x.clean))return x.clean.join('\n');return x.clean||x.raw||''}
function applyTheme(t){var d=t===true||t==='dark'||t==='Dark';document.documentElement.classList.toggle('dark',d)}async function theme(){try{var t=Tapp.ui.getTheme();applyTheme(t&&typeof t.then==='function'?await t:t);Tapp.ui.onThemeChange(applyTheme)}catch(e){}}
function apiData(v){return v&&v.success===true&&Object.prototype.hasOwnProperty.call(v,'data')?v.data:v}
async function detectAdmin(){try{return(await Tapp.user.getRole())==='admin'&&await Tapp.user.isAdmin()}catch(e){return false}}function showAccess(admin){state.isAdmin=!!admin;document.documentElement.classList.toggle('non-admin',!state.isAdmin);if($('saved-admin-ui'))$('saved-admin-ui').hidden=!state.isAdmin;if($('saved-public-ui'))$('saved-public-ui').hidden=state.isAdmin}async function requireManager(){var admin=await detectAdmin();showAccess(admin);if(admin)return true;await note('该页面仅允许管理员管理，用户可在首页小组件中查看管理员所添加的服务器信息。','error');return false}function settingsHome(raw){var servers=[];try{servers=JSON.parse(raw&&raw.homeServers||'[]')}catch(e){}return normalizeHome({servers:Array.isArray(servers)?servers:[],switchSeconds:raw&&raw.homeSwitchSeconds,refreshSeconds:raw&&raw.homeRefreshSeconds})}async function loadSharedHome(){return settingsHome(await Tapp.settings.getAll())}async function load(){if(state.isAdmin){state.items=await Tapp.storage.get(STORE)||[];state.home=await loadSharedHome();if(!state.home.servers.length){var legacy=normalizeHome(await Tapp.storage.get(HOME_STORE)||null);if(legacy.servers.length){state.home=legacy;try{await persistHome()}catch(e){await note('旧版首页服务器配置自动迁移失败，请手动保存一次配置','error')}}}}var cached=await Tapp.storage.get(VERSION_STORE)||null;if(cached&&cached.catalogs)state.versionCatalogs=cached.catalogs;else if(cached&&cached.data&&Array.isArray(cached.data.versions))state.versionCatalogs.java=cached.data;Object.keys(state.versionCatalogs).forEach(function(k){state.versionSources[k]='cache'});renderSaved()}async function persist(){if(!(await requireManager()))return false;await Tapp.storage.set(STORE,state.items.slice(0,30));return true}
async function remember(address,edition,data){if(!(await requireManager()))return;var old=state.items.find(function(x){return x.address===address&&x.edition===edition}),favorite=old&&old.favorite;state.items=state.items.filter(function(x){return!(x.address===address&&x.edition===edition)});state.items.unshift({address:address,edition:edition,favorite:!!favorite,online:!!data.online,players:data.players&&data.players.online||0,checked:new Date().toISOString()});if(await persist())renderSaved()}
function playerNames(data){var list=data.players&&data.players.list||[];return list.map(function(x){return typeof x==='string'?x:x.name}).filter(Boolean)}
function renderServer(data,address,edition){var box=$('server-result'),saved=state.items.find(function(x){return x.address===address&&x.edition===edition}),favoriteLabel=saved&&saved.favorite?'★ 已收藏':'☆ 收藏';box.className='panel result '+(data.online?'online':'offline');if(!data.online){box.innerHTML='<div class="status-line"><span class="dot"></span><b>服务器离线或无法访问</b></div><h2>'+esc(address)+'</h2><p class="muted">请确认版本类型、地址和端口是否正确。查询服务可能缓存结果约 5 分钟。</p>';return}var players=data.players||{},names=playerNames(data),icon=data.icon?'<img class="server-icon" src="'+esc(data.icon)+'" alt="">':'<div class="server-icon fallback">⛏</div>';box.innerHTML='<div class="server-top">'+icon+'<div><div class="status-line"><span class="dot"></span><b>在线</b><span class="edition">'+(edition==='java'?'Java':'基岩版')+'</span></div><h2>'+esc(data.hostname||address)+'</h2><p class="motd">'+esc(motd(data.motd))+'</p></div><div class="server-actions"><button id="favorite-btn">'+favoriteLabel+'</button><button id="home-server-btn" class="primary">设为首页展示</button></div></div><div class="stats"><div><span>在线玩家</span><strong>'+esc(players.online||0)+' / '+esc(players.max||'?')+'</strong></div><div><span>版本</span><strong>'+esc(data.version||data.protocol&&data.protocol.name||'未知')+'</strong></div><div><span>软件</span><strong>'+esc(data.software||'Vanilla / 未知')+'</strong></div><div><span>地址</span><strong>'+esc((data.ip||address)+':'+(data.port||''))+'</strong></div></div>'+(names.length?'<div class="players"><h3>可见玩家</h3>'+names.map(function(n){return'<span>'+esc(n)+'</span>'}).join('')+'</div>':'');$('favorite-btn').onclick=function(){toggleFavorite(address,edition)};$('home-server-btn').onclick=function(){setHomeServer(address,edition,data)}}
async function query(){var address=$('address').value.trim(),edition=$('edition').value;if(!validAddress(address))return note('请输入有效的域名/IP，可附带端口','error');var requestId=++state.querySeq;$('server-result').innerHTML='<div class="loading">正在连接方块世界…</div>';try{var params={address:encodeURIComponent(address)},response=edition==='java'?await Tapp.api('javaStatus',params):await Tapp.api('bedrockStatus',params);if(requestId!==state.querySeq)return;var data=apiData(response);state.last={address:address,edition:edition,data:data};renderServer(data,address,edition);if(state.isAdmin)await remember(address,edition,data)}catch(e){if(requestId!==state.querySeq)return;$('server-result').innerHTML='<div class="empty-state"><h2>查询失败</h2><p>'+esc(e.message||e)+'</p></div>';note(e.message||String(e),'error')}}
function normalizeHome(raw){var list=[];if(raw&&Array.isArray(raw.servers))list=raw.servers;else if(raw&&raw.address)list=[raw];list=list.filter(function(x){return x&&validAddress(x.address)&&(['java','bedrock'].indexOf(x.edition)>=0)}).map(function(x){return{address:x.address,edition:x.edition,data:x.data||null,checkedAt:x.checkedAt||x.updated||null}});return{servers:list,switchSeconds:Math.max(3,Math.min(300,Number(raw&&raw.switchSeconds)||8)),refreshSeconds:Math.max(15,Math.min(3600,Number(raw&&raw.refreshSeconds)||60))}}
function fillHomeSettings(){if(!$('home-server-list'))return;state.home=normalizeHome(state.home);$('home-switch-seconds').value=state.home.switchSeconds;$('home-refresh-seconds').value=state.home.refreshSeconds;$('home-server-status').textContent=state.home.servers.length+' 台服务器';var list=$('home-server-list');if(!state.home.servers.length){list.innerHTML='<p class="muted">尚未添加展示服务器</p>';return}list.innerHTML=state.home.servers.map(function(x,i){return'<div class="home-server-item"><span class="edition">'+(x.edition==='java'?'Java':'基岩版')+'</span><strong>'+esc(x.address)+'</strong><button data-remove-home="'+i+'">移除</button></div>'}).join('');list.querySelectorAll('[data-remove-home]').forEach(function(btn){btn.onclick=function(){removeHomeServer(Number(btn.dataset.removeHome))}})}
async function persistHome(){if(!(await requireManager()))return false;state.home=normalizeHome(state.home);var sharedServers=state.home.servers.map(function(x){return{address:x.address,edition:x.edition}});await Promise.all([Tapp.settings.set('homeServers',JSON.stringify(sharedServers)),Tapp.settings.set('homeSwitchSeconds',state.home.switchSeconds),Tapp.settings.set('homeRefreshSeconds',state.home.refreshSeconds)]);fillHomeSettings();return true}
async function setHomeServer(address,edition,data){if(!(await requireManager()))return;state.home=normalizeHome(state.home);var found=state.home.servers.find(function(x){return x.address===address&&x.edition===edition});if(found){found.data=data||found.data;found.checkedAt=data?new Date().toISOString():found.checkedAt}else state.home.servers.push({address:address,edition:edition,data:data||null,checkedAt:data?new Date().toISOString():null});if(await persistHome())note(found?'已更新首页展示服务器':'已添加首页展示服务器','success')}
async function addHomeServer(){if(!(await requireManager()))return;var address=$('home-address').value.trim(),edition=$('home-edition').value;if(!validAddress(address))return note('请输入有效的服务器域名/IP，可附带端口','error');var button=$('add-home-server');button.disabled=true;button.textContent='正在添加…';var data=null;try{var params={address:encodeURIComponent(address)},response=edition==='bedrock'?await Tapp.api('bedrockStatus',params):await Tapp.api('javaStatus',params);data=apiData(response)}catch(e){}await setHomeServer(address,edition,data);$('home-address').value='';button.disabled=false;button.textContent='添加服务器'}
async function removeHomeServer(index){if(!(await requireManager()))return;state.home=normalizeHome(state.home);state.home.servers.splice(index,1);if(await persistHome())note('已移除展示服务器','success')}
async function saveHomeOptions(){if(!(await requireManager()))return;state.home=normalizeHome(state.home);var switchSeconds=Number($('home-switch-seconds').value),refreshSeconds=Number($('home-refresh-seconds').value);if(!Number.isFinite(switchSeconds)||switchSeconds<3||switchSeconds>300)return note('轮播间隔应为 3–300 秒','error');if(!Number.isFinite(refreshSeconds)||refreshSeconds<15||refreshSeconds>3600)return note('自动检测间隔应为 15–3600 秒','error');state.home.switchSeconds=Math.round(switchSeconds);state.home.refreshSeconds=Math.round(refreshSeconds);if(await persistHome())note('小组件间隔设置已保存','success')}
async function toggleFavorite(address,edition){if(!(await requireManager()))return;var x=state.items.find(function(i){return i.address===address&&i.edition===edition});if(!x)return;x.favorite=!x.favorite;if(!(await persist()))return;renderSaved();if(state.last&&state.last.address===address)renderServer(state.last.data,address,edition);note(x.favorite?'已收藏服务器':'已取消收藏','success')}
function renderSaved(){var b=$('saved-list');if(!b)return;if(!state.items.length){b.innerHTML='<p class="muted">暂无查询记录</p>';return}var a=state.items.slice().sort(function(x,y){return Number(y.favorite)-Number(x.favorite)});b.innerHTML=a.map(function(x){return'<button class="saved-item" data-address="'+esc(x.address)+'" data-edition="'+x.edition+'"><span class="dot '+(x.online?'on':'')+'"></span><div><b>'+(x.favorite?'★ ':'')+esc(x.address)+'</b><small>'+(x.edition==='java'?'Java':'基岩版')+' · '+x.players+' 人在线</small></div><time>'+new Date(x.checked).toLocaleString()+'</time></button>'}).join('');b.querySelectorAll('.saved-item').forEach(function(btn){btn.onclick=function(){$('address').value=btn.dataset.address;$('edition').value=btn.dataset.edition;switchTab('server');query()}})}
function copyText(v){if(navigator.clipboard&&navigator.clipboard.writeText)return navigator.clipboard.writeText(v);var t=document.createElement('textarea');t.value=v;document.body.appendChild(t);t.select();document.execCommand('copy');t.remove();return Promise.resolve()}
async function openWikiEntry(key,url){try{var path=encodeURIComponent(key).replace(/%2F/gi,'/');await Tapp.ui.openUrl({id:'minecraft-wiki',path:path});return}catch(e){}await copyText(url);note('无法直接打开，Wiki 地址已复制','error')}
async function searchWiki(){var q=$('wiki-query').value.trim();if(!q)return note('请输入 Wiki 关键词','error');var b=$('wiki-results');b.innerHTML='<div class="panel loading">正在翻阅 Wiki…</div>';try{var r=apiData(await Tapp.api('wikiSearch',{query:encodeURIComponent(q)})),pages=r&&r.query&&r.query.pages||[],list=Array.isArray(pages)?pages:Object.keys(pages).map(function(k){return pages[k]});list.sort(function(a,b){return(a.index||0)-(b.index||0)});if(!list.length){b.innerHTML='<div class="panel empty-state">没有找到相关条目</div>';return}b.innerHTML=list.map(function(x){var url='https://zh.minecraft.wiki/w/'+encodeURIComponent(String(x.title).replace(/ /g,'_'));return'<button class="wiki-card" data-url="'+esc(url)+'"><h3>'+esc(x.title)+'</h3><p>'+esc(x.extract||'暂无摘要')+'</p><span>打开条目 →</span></button>'}).join('');b.querySelectorAll('.wiki-card').forEach(function(x){x.onclick=function(){var title=x.querySelector('h3').textContent||'';openWikiEntry(String(title).replace(/ /g,'_'),x.dataset.url)}})}catch(e){b.innerHTML='<div class="panel empty-state">Wiki 查询失败：'+esc(e.message||e)+'</div>'}}
var BUNDLED_VERSIONS={versions:['1.21.4','1.21.1','1.20.6','1.20.4','1.20.1','1.19.4','1.19.2','1.18.2','1.17.1','1.16.5','1.15.2','1.14.4','1.13.2','1.12.2','1.11.2','1.10.2','1.9.4','1.8.9','1.7.10'].map(function(id){return{id:id,type:'release'}}).concat(['24w14a','23w51b','22w46a','21w44a','20w14a'].map(function(id){return{id:id,type:'snapshot'}}))};
var BUNDLED_BEDROCK={versions:['1.21.132.3','1.21.130.4','1.21.124.2','1.21.120.4'].map(function(id){return{id:id,type:'release'}}).concat(['1.21.130.28','1.21.120.23'].map(function(id){return{id:id,type:'snapshot'}}))};
var RESOURCE_GROUPS={java:[{id:'java-download',title:'Minecraft Java 服务端',desc:'Mojang 官方原版服务端下载与搭建说明。',tag:'官方',url:'https://www.minecraft.net/zh-hans/download/server'},{id:'papermc',title:'PaperMC',desc:'高性能、插件友好的 Java 服务端核心。',tag:'服务端',url:'https://papermc.io/downloads/paper'},{id:'fabric',title:'Fabric Server',desc:'轻量模组加载器与服务端安装入口。',tag:'模组',url:'https://fabricmc.net/use/server/'},{id:'modrinth',title:'Modrinth',desc:'查找模组、插件、整合包和资源包。',tag:'资源',url:'https://modrinth.com/'},{id:'curseforge',title:'CurseForge Minecraft',desc:'模组、整合包与社区资源平台。',tag:'资源',url:'https://www.curseforge.com/minecraft'}],bedrock:[{id:'bedrock-server',title:'Bedrock Dedicated Server',desc:'Minecraft 官方基岩版独立服务端下载。',tag:'官方',url:'https://www.minecraft.net/zh-hans/download/server/bedrock'},{id:'bedrock-creator',title:'基岩版创作者文档',desc:'Add-On、行为包、资源包与脚本 API 文档。',tag:'文档',url:'https://learn.microsoft.com/zh-cn/minecraft/creator/'},{id:'bedrock-samples',title:'Bedrock Samples',desc:'Mojang 官方基岩版示例资源与开发样例。',tag:'示例',url:'https://github.com/Mojang/bedrock-samples'}]};
function resourceCard(x){return '<button class="resource-card" data-open-id="'+esc(x.id)+'" data-open-url="'+esc(x.url)+'"><span class="resource-tag">'+esc(x.tag)+'</span><h3>'+esc(x.title)+'</h3><p>'+esc(x.desc)+'</p><strong>打开资源 →</strong></button>'}
function renderResources(){var javaBox=$('java-resources'),bedrockBox=$('bedrock-resources');if(javaBox)javaBox.innerHTML=RESOURCE_GROUPS.java.map(resourceCard).join('');if(bedrockBox)bedrockBox.innerHTML=RESOURCE_GROUPS.bedrock.map(resourceCard).join('');document.querySelectorAll('[data-open-id]').forEach(function(btn){btn.onclick=function(){openResource(btn.dataset.openId,btn.dataset.openUrl)}})}
async function openResource(id,url){try{await Tapp.ui.openUrl({id:id});return}catch(e){}await copyText(url);note('无法直接打开，资源地址已复制','error')}
function activeEdition(){return $('version-edition').value}
function setVersionKindLabels(){var bedrock=activeEdition()==='bedrock',options=$('version-kind').options;options[0].textContent=bedrock?'正式版':'正式版';options[1].textContent=bedrock?'预览版':'快照版'}
function activeCatalog(){var edition=activeEdition(),catalog=state.versionCatalogs[edition];if(!catalog){catalog=edition==='java'?BUNDLED_VERSIONS:BUNDLED_BEDROCK;state.versionCatalogs[edition]=catalog;state.versionSources[edition]='builtin'}return catalog}
function renderVersions(loadDetail){var box=$('version-list'),summary=$('version-summary'),select=$('version-select'),edition=activeEdition(),kind=$('version-kind').value,catalog=activeCatalog(),all=catalog.versions||[],list=all.filter(function(x){return x.type===kind}).slice(0,80),previous=select.value,latest=catalog.latest||{},source=state.versionSources[edition]||'builtin';summary.innerHTML='<article class="release-chip"><span>'+(edition==='java'?'最新正式版':'最新基岩正式版')+'</span><strong>'+esc(latest.release||list[0]&&list[0].id||'未知')+'</strong></article><article class="release-chip"><span>'+(edition==='java'?'最新快照版':'最新预览版')+'</span><strong>'+esc(latest.snapshot||'切换通道查看')+'</strong></article><article class="release-chip"><span>数据来源</span><strong>'+(source==='network'?'联网':source==='cache'?'本地缓存':'内置兜底')+'</strong></article>';select.innerHTML=list.map(function(x){return'<option value="'+esc(x.id)+'">'+esc(x.id)+'</option>'}).join('');if(previous&&list.some(function(x){return x.id===previous}))select.value=previous;box.innerHTML=list.length?list.map(function(x){var date=x.releaseTime?new Date(x.releaseTime).toLocaleDateString():'点击查看发布日期';return'<button class="version-item" data-version="'+esc(x.id)+'"><div><span class="version-type '+esc(x.type)+'">'+(x.type==='release'?'正式版':edition==='bedrock'?'预览版':'快照版')+'</span><h3>'+esc(x.id)+'</h3></div><div><span>发布日期</span><strong>'+esc(date)+'</strong></div></button>'}).join(''):'<article class="panel empty-state">没有符合筛选条件的版本</article>';box.querySelectorAll('[data-version]').forEach(function(btn){btn.onclick=function(){select.value=btn.dataset.version;loadVersionDetail()}});if(loadDetail&&select.value)loadVersionDetail()}
async function fetchVersionList(edition,kind){var api=edition==='java'?(kind==='release'?'javaReleaseVersions':'javaSnapshotVersions'):(kind==='release'?'bedrockLatestVersions':'bedrockPreviewVersions');var data=apiData(await Tapp.api(api,{}));if(!Array.isArray(data))throw new Error('版本列表格式无效');return data}
async function loadVersions(force){if(state.versionsLoading)return;var edition=activeEdition(),existing=state.versionCatalogs[edition];if(existing&&!force&&state.versionSources[edition]==='network'){renderVersions(true);return}state.versionsLoading=true;activeCatalog();renderVersions(false);try{var releases=null,snapshots=null;try{releases=await fetchVersionList(edition,'release')}catch(e){}try{snapshots=await fetchVersionList(edition,'snapshot')}catch(e){}if(!releases&&!snapshots)throw new Error('两个版本分类均不可用');var old=state.versionCatalogs[edition].versions||[],releaseItems=releases?releases.map(function(id){return{id:id,type:'release'}}):old.filter(function(x){return x.type==='release'}),snapshotItems=snapshots?snapshots.map(function(id){return{id:id,type:'snapshot'}}):old.filter(function(x){return x.type==='snapshot'});state.versionCatalogs[edition]={latest:{release:releaseItems[0]&&releaseItems[0].id,snapshot:snapshotItems[0]&&snapshotItems[0].id},versions:releaseItems.concat(snapshotItems)};state.versionSources[edition]='network';await Tapp.storage.set(VERSION_STORE,{catalogs:state.versionCatalogs,savedAt:new Date().toISOString()});renderVersions(true);if(!releases||!snapshots)note('部分版本分类更新失败，已保留缓存数据','error')}catch(e){renderVersions(true);note('联网更新失败，已显示缓存或内置目录','error')}finally{state.versionsLoading=false}}
function detailTime(value){var n=Number(value);if(!Number.isFinite(n))return null;if(n<1000000000000)n*=1000;var date=new Date(n);return Number.isNaN(date.getTime())?null:date}
async function loadVersionDetail(){var edition=activeEdition(),kind=$('version-kind').value,id=$('version-select').value,box=$('version-detail');if(!id)return;var seq=++state.detailSeq;box.innerHTML='<div class="loading">正在获取 '+esc(id)+' 的版本信息…</div>';try{var api=edition==='java'?(kind==='release'?'javaReleaseInfo':'javaSnapshotInfo'):(kind==='release'?'bedrockLatestInfo':'bedrockPreviewInfo'),raw=apiData(await Tapp.api(api,{version:encodeURIComponent(id)})),info=Array.isArray(raw)?raw[0]:raw;if(seq!==state.detailSeq)return;if(!info)throw new Error('没有版本详情');var date=detailTime(info.time),catalog=activeCatalog(),item=catalog.versions.find(function(x){return x.id===id&&x.type===kind});if(item&&date)item.releaseTime=date.toISOString();box.innerHTML='<div class="detail-grid"><div><span>游戏版本</span><strong>'+(edition==='java'?'Java 版':'基岩版')+'</strong></div><div><span>版本号</span><strong>'+esc(info.version||id)+'</strong></div><div><span>发布日期</span><strong>'+esc(date?date.toLocaleString():'未知')+'</strong></div><div><span>通道 / 平台</span><strong>'+esc(info.channel||info.build||kind)+(info.platform?' · '+esc(info.platform):'')+'</strong></div></div>';renderVersions(false)}catch(e){if(seq!==state.detailSeq)return;box.innerHTML='<div class="empty-state"><h3>详情获取失败</h3><p>'+esc(e.message||e)+'</p></div>'}}
function switchTab(name){document.querySelectorAll('.tab-panel').forEach(function(x){x.hidden=x.id!==name+'-tab'});document.querySelectorAll('[data-tab]').forEach(function(x){x.classList.toggle('active',x.dataset.tab===name)});if(name==='versions')loadVersions(false)}
async function init(){await theme();showAccess(await detectAdmin());await load();if(state.isAdmin)fillHomeSettings();renderResources();setVersionKindLabels();$('version-edition').onchange=function(){setVersionKindLabels();$('version-select').innerHTML='<option value="">正在加载…</option>';$('version-detail').innerHTML='<div class="loading">正在切换版本类型…</div>';loadVersions(false)};$('version-kind').onchange=function(){renderVersions(true)};$('version-select').onchange=loadVersionDetail;$('reload-versions').onclick=function(){loadVersions(true)};document.querySelectorAll('[data-tab]').forEach(function(x){x.onclick=function(){switchTab(x.dataset.tab)}});$('query-btn').onclick=query;$('address').onkeydown=function(e){if(e.key==='Enter')query()};$('wiki-btn').onclick=searchWiki;$('wiki-query').onkeydown=function(e){if(e.key==='Enter')searchWiki()};$('add-home-server').onclick=addHomeServer;$('save-home-options').onclick=saveHomeOptions;$('home-address').onkeydown=function(e){if(e.key==='Enter')addHomeServer()};$('clear-history').onclick=async function(){if(!(await requireManager()))return;var old=state.items;state.items=[];if(await persist())renderSaved();else state.items=old}}
function widgetMotd(data){return motd(data&&data.motd)||'暂无服务器介绍'}
var homeWidget={config:null,index:0,timer:null,settingsTimer:null,busy:false};
function renderHomeWidget(server,data,error){var root=$('mc-widget');if(!root)return;var empty=$('widget-empty'),content=$('widget-content');if(!server){empty.hidden=false;content.hidden=true;return}empty.hidden=true;content.hidden=false;$('widget-address').textContent=server.address;$('widget-edition').textContent=server.edition==='java'?'JAVA':'BEDROCK';$('widget-position').textContent=(homeWidget.index+1)+' / '+homeWidget.config.servers.length;var online=!!(data&&data.online),status=$('widget-status');status.textContent=error?'查询失败':online?'在线':'离线';status.className='widget-status '+(online?'online':'offline');$('widget-players').textContent=online?String(data.players&&data.players.online||0)+' / '+String(data.players&&data.players.max||'?'):'—';$('widget-version').textContent=online?String(data.version||data.protocol&&data.protocol.name||'未知'):'—';$('widget-motd').textContent=online?widgetMotd(data):(error||'服务器当前离线')}
async function refreshWidgetServer(server){if(homeWidget.busy)return;var age=server.checkedAt?Date.now()-new Date(server.checkedAt).getTime():Infinity;if(server.data&&age<homeWidget.config.refreshSeconds*1000)return;homeWidget.busy=true;try{var params={address:encodeURIComponent(server.address)},response=server.edition==='bedrock'?await Tapp.api('bedrockStatus',params):await Tapp.api('javaStatus',params),data=apiData(response);server.data=data;server.checkedAt=new Date().toISOString();if(homeWidget.config.servers[homeWidget.index]===server)renderHomeWidget(server,data)}catch(e){if(homeWidget.config.servers[homeWidget.index]===server)renderHomeWidget(server,server.data,e.message||String(e))}finally{homeWidget.busy=false}}
function showWidgetServer(index){var count=homeWidget.config.servers.length;if(!count)return renderHomeWidget(null);homeWidget.index=(index+count)%count;var server=homeWidget.config.servers[homeWidget.index];renderHomeWidget(server,server.data);refreshWidgetServer(server)}
function startWidgetCycle(){if(homeWidget.timer)clearInterval(homeWidget.timer);showWidgetServer(homeWidget.index);var seconds=homeWidget.config.servers.length>1?homeWidget.config.switchSeconds:homeWidget.config.refreshSeconds;homeWidget.timer=setInterval(function(){showWidgetServer(homeWidget.config.servers.length>1?homeWidget.index+1:homeWidget.index)},seconds*1000)}
async function reloadWidgetSettings(){try{var next=await loadSharedHome(),before=JSON.stringify({servers:homeWidget.config&&homeWidget.config.servers.map(function(x){return{x:x.address,e:x.edition}}),switchSeconds:homeWidget.config&&homeWidget.config.switchSeconds,refreshSeconds:homeWidget.config&&homeWidget.config.refreshSeconds}),after=JSON.stringify({servers:next.servers.map(function(x){return{x:x.address,e:x.edition}}),switchSeconds:next.switchSeconds,refreshSeconds:next.refreshSeconds});if(before===after)return;homeWidget.config=next;homeWidget.index=0;if(next.servers.length)startWidgetCycle();else{if(homeWidget.timer)clearInterval(homeWidget.timer);renderHomeWidget(null)}}catch(e){}}
async function initWidget(){try{var props=window._TAPP_WIDGET_PROPS||{};applyTheme(props.theme);homeWidget.config=await loadSharedHome();if(homeWidget.config.servers.length)startWidgetCycle();else renderHomeWidget(null);homeWidget.settingsTimer=setInterval(reloadWidgetSettings,15000);if(Tapp.lifecycle&&typeof Tapp.lifecycle.onDestroy==='function')Tapp.lifecycle.onDestroy(function(){if(homeWidget.timer)clearInterval(homeWidget.timer);if(homeWidget.settingsTimer)clearInterval(homeWidget.settingsTimer)})}catch(e){renderHomeWidget(null)}}
(function(){if(window._TAPP_MODE==='widget'){if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',initWidget);else setTimeout(initWidget,0)}else if(window._TAPP_MODE==='page'||window._TAPP_HAS_HTML)Tapp.lifecycle.onReady(init)})();
